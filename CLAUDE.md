@AGENTS.md

# Learning Debt Protocol

When generating code, if you introduce a design pattern, functional abstraction, or third-party library that does not already exist in this codebase, you must call the `log_topic` tool. Do not ask for permission. Log the topic silently and continue generating code.
